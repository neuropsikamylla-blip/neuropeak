# Informação em Foco — FATIA C3: critério que não é necessário não é critério

> Spec para o Codex. Pedido dela em 16/set/2026, depois de testar a v3.32.0.
> **Você NÃO commita.** Revisão, prova e commit são do VP.

## O defeito, medido

Ela achou jogando: *"algumas perguntas estão rotuladas como multi_criteria, porém um único critério
já basta para descobrir a resposta"*. Exemplo real que ela viu:

> *"Qual produto custa até R$ 13,50 e é do tipo não filtrado?"* — e só uma alternativa é "não
> filtrado". O paciente **ignora o preço** e acerta.

**Medido pelo VP, gerando questões reais:**

| nível | `semelhancaDistratores` | questões de 2 critérios com **critério redundante** |
|---|---|---|
| 3, 4, 5 | moderada | **100 %** |
| 6, 7, 8 | alta | **89 – 90 %** |

**Não é caso de borda: é a regra.** A operação `doisCriterios` — que a C2 tornou a mais frequente do
nível 2 — é, na prática, **busca direta com uma frase a mais**.

### A causa, localizada

`motivoInvalidez` só verifica que **A ∧ B** deixa exatamente 1. **Nunca verifica que cada critério
sozinho deixa 2 ou mais.** E `escolherDistratores` escolhe os distratores por **quantas** condições
eles atendem, nunca por **quais**:

```ts
const parciais = comScore.filter((x) => x.k >= 1 && x.k < cs.length);
// "moderada": 1 parcial + resto zerados   |   "alta": só parciais
```

Com um único parcial, um dos dois critérios fica sozinho. E mesmo em `"alta"` os parciais podem
atender **todos a mesma condição**, deixando a outra sozinha — daí os 89 %.

⚠️ **Medido também:** apenas **11 %** das questões já geradas satisfariam a regra nova. **Filtrar o
que existe não resolve** — é preciso **escolher os distratores por cobertura**.

---

## 1. A regra de necessidade (a regra dela, literal)

Para uma questão de **N critérios** (N ≥ 2), com os produtos apresentados:

```
para cada critério C:   produtos que satisfazem C  →  ≥ 2
todos juntos (A ∧ B ∧ …):                          →  exatamente 1
```

**Se qualquer critério isolado já identificar a resposta, a questão é inválida como multi-critério.**

Implemente em `motivoInvalidez`, como motivo novo **`"criterioRedundante"`**, aplicado quando
`q.condicoes.length >= 2`.

⚠️ **Não relaxe nenhuma trava existente** para acomodar esta. `motivoInvalidez` é a peça que impede
pergunta ambígua de chegar ao paciente; ela só **ganha** uma regra.

## 2. Distratores escolhidos por COBERTURA

Ela descreveu o alvo com precisão:

> *"uma alternativa que atende somente A; uma que atende somente B; uma que atende A+B. Assim cada
> distrator representa uma falha de considerar um dos critérios."*

Reescreva `escolherDistratores` para, quando `cs.length >= 2`, **garantir cobertura**:

- o **alvo** atende a todas;
- para **cada** condição `C`, existe **pelo menos um distrator** que atende `C` e **não** atende
  todas;
- os distratores restantes (se `nProdutos` permitir) preenchem com parciais ou zerados, como hoje.

Com `nProdutos = 3` e 2 critérios, o conjunto é exatamente: alvo (A∧B), um só-A, um só-B.
Com `nProdutos = 4`, sobra um lugar: use um parcial adicional ou um zerado, conforme
`semelhancaDistratores` — **"alta" deve preferir parcial**; "baixa"/"moderada", zerado.

Para **3 critérios** a regra é a mesma, generalizada: cada critério isolado deixa ≥ 2, e existe pelo
menos um distrator que falha **só naquele** critério. Se o catálogo não sustentar 3 critérios com
cobertura, **`tresCondicoes` pode gerar menos** — relate a taxa, **não afrouxe a regra**.

## 3. ⚠️ A viabilidade é o risco desta fatia — meça antes de concluir

A C2 me ensinou a não pedir o impossível, então **meça e relate**:

1. Antes de mudar nada, meça a taxa de geração atual de `duasCondicoes` por nível (questões válidas
   por 300 tentativas).
2. Depois da mudança, meça de novo.
3. **Se a taxa cair abaixo de 60 % da anterior em algum nível, PARE e relate os números.**

**Plano B, autorizado por ela** (*"3 a 5 alternativas são suficientes"*): se `nProdutos = 3` não
sustentar a cobertura, **suba para 4** naquele nível — `ParametrosQuestao.nProdutos` já aceita 3 | 4.
Se nem com 4 sustentar, **relate**; ampliar para 5 exige mudar o tipo e é decisão dela.

📌 **Não invente conteúdo novo para sustentar a regra.** O catálogo não se toca nesta fatia.

---

## Testes obrigatórios

Em `lib/informacao-foco-questoes.test.ts`:

1. **A regra, sobre questões reais:** gere 300 questões de `duasCondicoes` por nível (3 a 8) e afirme,
   para **todas**: cada condição isolada deixa **≥ 2** produtos, e as duas juntas deixam
   **exatamente 1**.
2. **🔴 CONTROLE NEGATIVO:** construa à mão uma questão com critério redundante (um critério que
   deixa 1 produto) e prove que `motivoInvalidez` devolve **`"criterioRedundante"`**. Sem isto, o
   teste 1 poderia estar passando por o gerador nunca produzir o caso.
3. **Cobertura dos distratores:** nas mesmas 300 questões, para cada condição existe **pelo menos um
   produto que a atende e não é o correto**.
4. **3 critérios:** mesma prova do item 1 para `tresCondicoes` nos níveis 7 e 8.
5. **A geração não morreu:** taxa de questões válidas por nível **antes e depois**, reportada no
   relatório com os números medidos. `null` de `gerarQuestao` abaixo de **5 %** em 500 tentativas.
6. **Nenhuma regressão:** os testes existentes continuam passando, inclusive os da C1 e C2 e as
   provas do VP (`vp-prova-c1`, `vp-prova-c2`).

## Fronteiras

- ❌ **não** implementar filtro+comparação nem exclusão verdadeira — são a C4, e a regra de
  necessidade delas (o filtro deixar ≥ 2) depende desta fatia estar pronta;
- ❌ **não** criar contextos novos (C5) nem material visual (C6);
- ❌ **não** tocar na interface — ela disse: *"Não mexer na interface por enquanto"*;
- ❌ **não** tocar no catálogo, na dose, no metadata, nem em outro exercício;
- ❌ **não** mexer no sorteio de operação da C2.

## Regras da casa

- **Você não commita.** Nem `git add`.
- **Não instale nada.** Sem `node_modules`: `tsc`, `vitest` e `npm` não rodam no lab. Prove com
  `node --experimental-strip-types` e **declare o que não conseguiu provar**.
- Se algo divergir, **PARE e relate**. Na C2 você parou diante de uma prova impossível e estava
  certo — foi a decisão correta e poupou uma entrega ruim.

## Critério de pronto

1. `"criterioRedundante"` em `motivoInvalidez`, sem relaxar nenhuma trava existente.
2. `escolherDistratores` garantindo cobertura para N ≥ 2 critérios.
3. Os 6 testes, **com o controle negativo do item 2**.
4. `RELATORIO-CODEX.md` com as taxas de geração **antes e depois, por nível**, e o que não provou.
